// Configuration for DeepSeek API
// Note: In production, you should use a backend server to protect your API key
// This is for educational demo purposes only

export const config = {
    // You can add your API key here temporarily for demo
    // OR use a simple backend proxy (recommended)
    apiKey: '', // Leave empty and use prompt input
    apiUrl: 'https://api.deepseek.com/v1/chat/completions',
    model: 'deepseek-chat'
};
